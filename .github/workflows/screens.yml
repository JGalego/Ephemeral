name: Screens

# Photographs the Android application, running.
#
# Nobody has ever seen these screens. The engine is tested to death, the JNI
# bridge is driven from a real JVM on every commit, and the Kotlin now compiles
# on every commit too — but "it compiles" is not "somebody looked at it", and
# the desktop window taught this repository exactly what lives in that gap: the
# first film of it found six bugs no test had.
#
# There is no phone here and there is no phone in CI. An emulator is not a
# device and this workflow does not pretend otherwise — what it produces is a
# picture of the real application, drawn by the real Android framework, which
# is the closest thing to looking that a repository can automate.
#
# Deliberately outside the required checks. It needs KVM and it takes minutes,
# and a screenshot is something a person reviews rather than something that
# passes or fails.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "apps/android/**"
      - "crates/ephemeral-design/**"
      - ".github/workflows/screens.yml"

permissions:
  contents: read

# Deliberately not cancel-in-progress. This takes twenty minutes, and on a
# branch anybody is working on, cancelling it whenever the next commit lands
# means it never once reaches the end — which is exactly what happened: two
# runs in a row were killed at the boot step by a push that touched nothing
# Android at all.
concurrency:
  group: screens-${{ github.ref }}
  cancel-in-progress: false

jobs:
  android:
    name: The Android application, running
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      # Where the AVD goes, said once for every step rather than left to
      # whichever of three environment variables each tool happens to prefer.
      # `avdmanager` and `emulator` disagreed about that, which cost two runs:
      # the AVD was created successfully and the emulator then said "Unknown
      # AVD name [ephemeral]" and listed the three directories it had looked
      # in, none of which was the one it had been written to.
      ANDROID_AVD_HOME: ${{ github.workspace }}/.avd
    steps:
      - uses: actions/checkout@v4

      # Made first, because the step that saves the log runs even when an
      # earlier one failed — and it failed, on nothing but a missing directory.
      - run: mkdir -p screens

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.14.3

      # The engine, for the architecture the emulator is.
      #
      # Without this the APK builds and installs perfectly and then says
      # `dlopen failed: library "libephemeral_android.so" not found` on its
      # first screen — which is what the first successful run of this workflow
      # photographed. The application handled it gracefully, which was worth
      # seeing; it also meant nothing past the front door could be.
      - name: The engine, for the emulator
        run: |
          rustup target add x86_64-linux-android wasm32-wasip1
          EPHEMERAL_ANDROID_ABIS=x86_64 ./build-native.sh
          ./build-interpreters.sh
        working-directory: apps/android
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_NDK_LATEST_HOME }}

      - name: Build the application
        run: gradle assembleDebug --no-daemon
        working-directory: apps/android

      # An emulator needs hardware virtualisation to be usable. The runner has
      # it; the container this repository is often worked in does not, which is
      # the whole reason these screens had never been seen.
      #
      # Existing is not the same as being usable. `/dev/kvm` is present on the
      # runner and owned by a group the job's user is not in, so the emulator
      # started, ran for fifteen minutes, and then said "x86_64 emulation
      # currently requires hardware acceleration!" — while a check that only
      # asked whether the file was there reported everything was fine.
      - name: Make KVM usable
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm-for-this-job.rules > /dev/null
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Check KVM is usable
        run: |
          ls -l /dev/kvm || true
          if [ ! -w /dev/kvm ]; then
            echo "::error::/dev/kvm is not writable by $(id -un), so the emulator cannot accelerate."
            id
            exit 1
          fi
          echo "KVM is available and writable."

      # Driven through the SDK's own tools rather than a third-party action.
      # A screenshot is not worth adding somebody else's code to the pipeline
      # that also signs releases.
      - name: An emulator
        run: |
          set -eu

          # Found rather than assumed: the runner image keeps its command-line
          # tools under a versioned directory, and `latest` is not always one
          # of them.
          sdkmanager="$(command -v sdkmanager || true)"
          if [ -z "$sdkmanager" ]; then
            sdkmanager="$(ls "$ANDROID_HOME"/cmdline-tools/*/bin/sdkmanager 2>/dev/null | head -1)"
          fi
          if [ -z "$sdkmanager" ]; then
            echo "No sdkmanager under $ANDROID_HOME:"
            ls -R "$ANDROID_HOME/cmdline-tools" 2>/dev/null || true
            exit 1
          fi
          echo "Using $sdkmanager"
          avdmanager="$(dirname "$sdkmanager")/avdmanager"

          # `yes |` deliberately without pipefail: `yes` is killed by SIGPIPE
          # the moment sdkmanager stops reading, which is a non-zero exit for
          # a pipeline that did exactly what it was meant to. That, and not
          # anything about Android, is what failed the first time this ran.
          yes | "$sdkmanager" --licenses > /dev/null || true

          "$sdkmanager" "platform-tools" "emulator" \
            "system-images;android-34;google_apis;x86_64"

          mkdir -p "$ANDROID_AVD_HOME"
          echo "no" | "$avdmanager" create avd --name ephemeral \
            --package "system-images;android-34;google_apis;x86_64" --force

          echo "$ANDROID_HOME/emulator" >> "$GITHUB_PATH"
          echo "$ANDROID_HOME/platform-tools" >> "$GITHUB_PATH"

          # Proved rather than assumed. `create avd` exiting zero is not the
          # same as the emulator being able to find what it made, and the
          # difference between those two is fifteen minutes of waiting for a
          # device that was never going to appear.
          if ! "$ANDROID_HOME/emulator/emulator" -list-avds | grep -qx ephemeral; then
            echo "::error::The AVD was created but the emulator cannot see it."
            echo "ANDROID_AVD_HOME=$ANDROID_AVD_HOME"
            ls -la "$ANDROID_AVD_HOME" || true
            "$ANDROID_HOME/emulator/emulator" -list-avds || true
            exit 1
          fi
          echo "The emulator can see the AVD."

      # Every wait here is bounded. `adb wait-for-device` blocks forever when
      # the emulator died on startup, and a step that hangs until the job
      # timeout tells you nothing about why — it just costs forty minutes.
      - name: Boot it
        run: |
          set -uo pipefail
          emulator -avd ephemeral -no-window -no-audio -no-boot-anim \
            -no-snapshot -gpu swiftshader_indirect -accel on \
            -camera-back none -camera-front none \
            -netdelay none -netspeed full > screens/emulator.log 2>&1 &

          # Fifteen minutes, because seven was a guess and it was wrong: a cold
          # boot of a fresh AVD on a two-core runner with a software GPU is
          # slow, and the first run to hit this limit had not failed — it was
          # still coming up. Progress is printed on the way, so a log that ends
          # here says whether anything was happening.
          waited=0
          until timeout 20 adb shell getprop sys.boot_completed 2>/dev/null | grep -q 1; do
            sleep 10
            waited=$((waited + 10))

            if [ $((waited % 60)) -eq 0 ]; then
              echo "  ${waited}s: $(timeout 10 adb devices | tail -n +2 | tr '\n' ' ')"
            fi

            if [ "$waited" -ge 900 ]; then
              echo "::error::The emulator did not finish booting in fifteen minutes."
              timeout 20 adb devices || true
              tail -80 screens/emulator.log || true
              exit 1
            fi
          done

          echo "Booted after ${waited}s."
          adb shell wm dismiss-keyguard || true
          adb shell settings put global window_animation_scale 0 || true
          adb shell settings put global transition_animation_scale 0 || true
          adb shell settings put global animator_duration_scale 0 || true

      # The walkthrough is a script in the repository rather than a wall of
      # adb calls in a YAML file, for the same reason `film.mjs` is: it is the
      # same command here and on anybody's machine with an emulator, and it is
      # reviewable.
      - name: Photograph it
        run: |
          adb install -r apps/android/app/build/outputs/apk/debug/app-debug.apk
          apps/android/tests/photograph.py screens

      - name: What it said while doing it
        if: always()
        run: |
          timeout 60 adb logcat -d -s Ephemeral:* AndroidRuntime:E \
            > screens/logcat.txt 2>&1 || echo "(no device to ask)" > screens/logcat.txt

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: android-screens
          path: screens/
          if-no-files-found: warn
