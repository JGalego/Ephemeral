<?xml version="1.0" encoding="utf-8"?>
<!-- An application waiting on a decision. The only thing in the list that
     shouts, because it is the only thing that needs a person. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/high" />
    <corners android:radius="999dp" />
</shape>
