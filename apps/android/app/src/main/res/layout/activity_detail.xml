<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ground"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/purpose"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/ink"
            android:textSize="21sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/pill"
            android:layout_marginTop="10dp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="3dp"
            android:textColor="@color/ink_quiet"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/explanation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="12dp"
            android:textColor="@color/ink"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="6dp"
            android:textColor="@color/ink_quiet"
            android:textSize="13sp" />

        <Button
            android:id="@+id/generate"
            style="@style/EphemeralButton.Primary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:text="@string/generate" />

        <TextView
            android:id="@+id/status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@color/medium_soft"
            android:padding="12dp"
            android:textColor="@color/medium"
            android:textSize="14sp"
            android:visibility="gone" />

        <!--
          Set in code rather than here: what a phone can do with an application
          depends on the application, and a fixed sentence was wrong for half of
          them the moment WebAssembly arrived.
        -->
        <TextView
            android:id="@+id/where_it_runs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="12dp"
            android:textColor="@color/ink_quiet"
            android:text="@string/phone_limits"
            android:textSize="12sp" />

        <!--
          What the application says it takes, drawn as a form.

          Above the permissions rather than below: the question "what do I give
          it" comes before "what may it reach", and a person filling this in has
          already decided to run the thing.
        -->
        <LinearLayout
            android:id="@+id/form"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="8dp" />

        <LinearLayout
            android:id="@+id/permissions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="8dp" />

        <TextView
            android:id="@+id/footnotes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingMultiplier="1.25"
            android:paddingTop="20dp"
            android:textColor="@color/ink_quiet"
            android:textSize="13sp" />
    </LinearLayout>
</ScrollView>
